FROM nginx:alpine

# Copy frontend files
COPY . /usr/share/nginx/html

# Remove any development environment files
RUN rm -f /usr/share/nginx/html/.env* /usr/share/nginx/html/node_modules

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
